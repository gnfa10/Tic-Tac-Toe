// function declarations header file

void introduction(); 
void take_turn(); 
bool is_winner(); // checks for winner
bool is_filled(); // continously check if board is filled
void set_position(); //sets the input from user in the right location in array based on i/p
void update_board(); //checks if input is from user 1 or 2 and updates board with 'X' or 'O'
void change_player(); // iterate player turn
void draw(); // output board with user i/p 
void end_game();
